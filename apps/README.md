# Apps

This directory contains all the applications in the MixMatch monorepo.

## Structure

- `api/` - Backend API server (NestJS)
- `stellar-service/` - Stellar blockchain integration service
- `web/` - Next.js web application
- `mobile/` - React Native mobile application (Expo)
